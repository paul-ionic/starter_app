import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.cs.demo.dev',
  appName: 'starter_app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
